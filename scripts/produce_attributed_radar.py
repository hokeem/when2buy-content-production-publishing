#!/usr/bin/env python3
"""Deprecated guard: the old attributed text-card producer must never run."""
raise SystemExit(
    "produce_attributed_radar.py is disabled. Use the image-generation model, "
    "apply the exact when2buy logo, record visualProduction metadata, and run "
    "scripts/validate_content_standard.py before publishing."
)
