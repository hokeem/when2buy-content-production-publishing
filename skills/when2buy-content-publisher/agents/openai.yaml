interface:
  display_name: "when2buy 内容制作与发布"
  short_description: "扫描双对标账号，复刻同选题并生成 when2buy 图文"
  default_prompt: "Use $when2buy-content-publisher to scan @WhaleInsider and @StockMKTNewz first, then produce the strongest same-topic when2buy package."
